document.addEventListener("DOMContentLoaded", function(){
    //do whatever has to happen first
    //load free_trial info
    //get fetch request to back end, lives in API class
    API.addFamilies()
})